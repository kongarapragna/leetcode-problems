class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n = nums.size();
        int nSum = n * (n + 1) / 2;
        int actSum = 0;
        for(int i : nums) {
            actSum += i;
        }
        return nSum - actSum;
    }
};